{
  "name": "neon-velocity-midi-game",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "notes": "node scripts/generate_note_data.mjs output/neon_velocity_edm.mid public/charts/neon-velocity.json",
    "audio:notes": "node scripts/generate_audio_note_data.mjs",
    "audio:analyze": "node scripts/analyze_audio_file.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify:browser": "node scripts/verify_browser.mjs"
  },
  "dependencies": {
    "@tonejs/midi": "^2.0.28",
    "three": "^0.179.1"
  },
  "devDependencies": {
    "puppeteer-core": "^25.8.0",
    "vite": "^7.1.3"
  }
}
